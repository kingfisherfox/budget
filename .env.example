# Optional local overrides only. The API gets DATABASE_URL from docker-compose.yml.
# Postgres is exposed on the host at port 5961 (maps to 5432 inside the container).
# Create users with Sign up at http://localhost:8081/account — not via this file.
