node_modules/
dist/

# Secrets only — copy `.env.example` → `.env` for local use.
.env
.env.local
# Explicitly track the template even if a global gitignore uses a broad `.env*` pattern.
!.env.example
*.log
.DS_Store

# Not tracked (local copies only)
tests/
scripts/

# Optional per-machine Docker Compose overrides (same directory as docker-compose.yml)
docker-compose.override.yml
